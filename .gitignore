.env
airbnb_app/amesterdam/
# Créé par https://www.toptal.com/developers/gitignore/api/python,django,linux
# Modifier à https://www.toptal.com/developers/gitignore?templates=python,django,linux

### Django ###
*.journal
*.pot
*.pyc
__pycache__/
local_settings.py
db.sqlite3
db.sqlite3-revue
les médias

# Si votre processus de génération comprend l'exécution des collectstatic, alors vous avez probablement n'avez pas besoin ou souhaitez inclure staticfiles/
# dans votre dépôt Git. Mise à jour et décommentez la ligne suivante en conséquence.
# <django-projet-nom>/staticfiles/

### Django.Pile De Python ###
# Byte-compilé / optimisé / fichiers DLL
*.py[cod]
*$py.class

# C extensions
*.donc

# Distribution / emballage
.Python
construire/
développer des œufs/
dist/
téléchargements/
œufs/
.œufs/
lib/
lib64/
pièces de rechange/
sdist/
var/
roues/
partager/python-roues/
*.egg-info/
.installé.cfg
*.oeuf
MANIFESTE

# PyInstaller
# Habituellement, ces fichiers sont écrits par un script python à partir d'un modèle
# avant de PyInstaller construit le exe, de manière à injecter de date/d'autres infos en elle.
*.manifeste
*.spec

# Journaux du programme d'installation
pip-log.txt
pip-delete-this-directory.txt

# Test de l'unité / des rapports de couverture de
htmlcov/
.tox/
.nox/
.la couverture
.de la couverture.*
.cache
nosetests.xml
coverage.xml
*.couvrir
*.py,couverture
.hypothèse/
.pytest_cache/
couvercle/

# Traductions
*.mo

# Django trucs:

# Fiole de trucs:
instance/
.webassets-cache

# Scrapy trucs:
.scrapy

# Sphinx de la documentation
docs/_build/

# PyBuilder
.pybuilder/
cible/

# Jupyter Portable
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
# Pour une bibliothèque ou un package, vous pouvez ignorer ces fichiers depuis le code est
# destinées à fonctionner dans de multiples environnements; sinon, allez dans:
# .python-version

# pipenv
# Selon pypa/pipenv#598, il est recommandé d'inclure Pipfile.lock dans le contrôle de version.
# Toutefois, en cas de collaboration, si le fait d'avoir une plate-forme spécifique des dépendances ou des dépendances
# ayant pas de croix-plate-forme de soutien, pipenv peut installer des dépendances qui ne fonctionnent pas, ou pas
# installer toutes les dépendances nécessaires.
#Pipfile.verrouillage

# poésie
# Similaire à Pipfile.de verrouillage, il est généralement recommandé d'inclure la poésie.lock dans le contrôle de version.
# Ceci est particulièrement recommandé pour les paquets binaires pour assurer la reproductibilité, et est plus
# généralement ignorées pour les bibliothèques.
# https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poésie.verrouillage

# pdm
# Similaire à Pipfile.de verrouillage, il est généralement recommandé d'inclure pdm.lock dans le contrôle de version.
#pdm.verrouillage
# pdm magasins à l'échelle du projet de configurations .pdm.toml, mais il est recommandé de ne pas l'inclure
# dans le contrôle de version.
# https://pdm.fming.dev/#use-with-ide
.pdm.toml

# PEP 582; utilisés par exemple par les github.com/David-OConnor/pyflow et github.com/pdm-project/pdm
__pypackages__/

# Céleri trucs
celerybeat-annexe
celerybeat.pid

# SageMath analysé les fichiers
*.sage.py

# Environnements
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder paramètres du projet
.spyderproject
.spyproject

# Corde paramètres du projet
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Bûcher type checker
.bûcher/

# pytype de type statique de l'analyseur
.pytype/

# Cython symboles de débogage
cython_debug/

# PyCharm
# JetBrains modèle spécifique est maintenu dans un autre JetBrains.gitignore qui peut
# être trouvé à https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
# et peut être ajouté au global gitignore ou fusionné dans ce fichier. Pour plus de nucléaire
# option (non recommandé), vous pouvez décommenter la suite d'ignorer l'idée entière dossier.
#.idée/

### Linux ###
*~

# les fichiers temporaires qui peuvent être créés si un processus a encore une poignée de l'ouverture d'un fichier supprimé
.fuse_hidden*

# KDE répertoire des préférences
.annuaire

# Linux dossier corbeille qui peut apparaître sur n'importe quel disque ou de partition
.Corbeille-*

# .nfs fichiers sont créés lors de l'ouvrir un fichier est supprimé mais il est encore accessible
.nfs*

### Python ###
# Byte-compilé / optimisé / fichiers DLL

# C extensions

# Distribution / emballage

# PyInstaller
# Habituellement, ces fichiers sont écrits par un script python à partir d'un modèle
# avant de PyInstaller construit le exe, de manière à injecter de date/d'autres infos en elle.

# Journaux du programme d'installation

# Test de l'unité / des rapports de couverture de

# Traductions

# Django trucs:

# Fiole de trucs:

# Scrapy trucs:

# Sphinx de la documentation

# PyBuilder

# Jupyter Portable

# IPython

# pyenv
# Pour une bibliothèque ou un package, vous pouvez ignorer ces fichiers depuis le code est
# destinées à fonctionner dans de multiples environnements; sinon, allez dans:
# .python-version

# pipenv
# Selon pypa/pipenv#598, il est recommandé d'inclure Pipfile.lock dans le contrôle de version.
# Toutefois, en cas de collaboration, si le fait d'avoir une plate-forme spécifique des dépendances ou des dépendances
# ayant pas de croix-plate-forme de soutien, pipenv peut installer des dépendances qui ne fonctionnent pas, ou pas
# installer toutes les dépendances nécessaires.

# poésie
# Similaire à Pipfile.de verrouillage, il est généralement recommandé d'inclure la poésie.lock dans le contrôle de version.
# Ceci est particulièrement recommandé pour les paquets binaires pour assurer la reproductibilité, et est plus
# généralement ignorées pour les bibliothèques.
# https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control

# pdm
# Similaire à Pipfile.de verrouillage, il est généralement recommandé d'inclure pdm.lock dans le contrôle de version.
# pdm magasins à l'échelle du projet de configurations .pdm.toml, mais il est recommandé de ne pas l'inclure
# dans le contrôle de version.
# https://pdm.fming.dev/#use-with-ide

# PEP 582; utilisés par exemple par les github.com/David-OConnor/pyflow et github.com/pdm-project/pdm

# Céleri trucs

# SageMath analysé les fichiers

# Environnements

# Spyder paramètres du projet

# Corde paramètres du projet

# mkdocs documentation

# mypy

# Bûcher type checker

# pytype de type statique de l'analyseur

# Cython symboles de débogage

# PyCharm
# JetBrains modèle spécifique est maintenu dans un autre JetBrains.gitignore qui peut
# être trouvé à https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
# et peut être ajouté au global gitignore ou fusionné dans ce fichier. Pour plus de nucléaire
# option (non recommandé), vous pouvez décommenter la suite d'ignorer l'idée entière dossier.

### Python Patch ###
# Poésie fichier de configuration local - https://python-poetry.org/docs/configuration/#local-configuration
de la poésie.toml


# Fin de la https://www.toptal.com/developers/gitignore/api/python,django,linux
 